// Phase 0 placeholder for the timeline strip. Real ruler + tracks + clips
// land in P1.7 — see /docs/spec.md §11.

export function TimelinePlaceholder(): JSX.Element {
  return (
    <footer className="timeline-placeholder">
      <div className="timeline-placeholder__label mono">Timeline — P1.7</div>
    </footer>
  );
}
