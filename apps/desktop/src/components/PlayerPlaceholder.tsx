// Phase 0 placeholder for the centre column. The real PlayerStage with
// embedded `hyperframes preview` arrives in P0.7-0.8.

export function PlayerPlaceholder(): JSX.Element {
  return (
    <main className="player-placeholder">
      <div className="player-placeholder__frame">
        <div className="player-placeholder__caption mono">PlayerStage — P0.7</div>
      </div>
    </main>
  );
}
